{
  "name": "rds_performance_insights",
  "description": "RDS Performance Insights top SQL by AAS.",
  "input_schema": {
    "type": "object",
    "required": ["db_id", "start", "end"],
    "properties": {
      "db_id":    {"type": "string"},
      "start":    {"type": "string"},
      "end":      {"type": "string"},
      "group_by": {"type": "string", "description": "PI group prefix: db.sql_tokenized | db.wait_event | db.host | db.user | db.application"}
    }
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "top_sql": {"type": "array"}
    }
  }
}
