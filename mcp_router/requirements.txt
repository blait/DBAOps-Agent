# MCP Router — 커스텀 핸들러 boto3 + stdio MCP 서버들의 합집합 의존성.
# (mcp_tools/*/requirements.txt 를 한 컨테이너에 통합)

# MCP client SDK (stdio_client / ClientSession)
mcp>=1.2.0

# 커스텀 핸들러 4개가 쓰는 boto3
boto3>=1.34.0

# stdio MCP 서버 (python) — router 컨테이너 안에서 spawn 됨
postgres-mcp>=0.3.0
prometheus-mcp-server>=1.6.1
awslabs.cloudwatch-mcp-server>=0.1.2
awslabs.aws-documentation-mcp-server>=1.1.24
awslabs.aws-api-mcp-server>=1.3.36
